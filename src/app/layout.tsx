import type { Metadata } from "next";
import { Inter, Playfair_Display } from "next/font/google";
import "./globals.css";

const inter = Inter({ subsets: ["latin"], variable: "--font-inter" });
const playfair = Playfair_Display({ subsets: ["latin"], variable: "--font-playfair" });

export const metadata: Metadata = {
  title: "Nexus Concierge | The Pre-eminent Social Concierge",
  description: "Experience a culture of service. Nexus coordinates luxury retreats, group dining, and elite transit with effortless delegation.",
  keywords: ["luxury concierge", "group travel", "private concierge", "nexus concierge", "bangkok luxury"],
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body className={`${inter.variable} ${playfair.variable} antialiased`}>
        {children}
      </body>
    </html>
  );
}
